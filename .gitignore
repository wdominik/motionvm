# Build output
/target/

# macOS
.DS_Store

# motionvm-motion-tools writes extracted game material into the working
# directory by default — that material must never enter the repository.
/out/
/sprite-*.png
