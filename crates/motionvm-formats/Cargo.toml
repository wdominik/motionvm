[package]
name = "motionvm-formats"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Readers for the MOTION engine's resource formats"

[dependencies]

# Deliberately empty above: this crate reads the game's formats with nothing but
# std. The dependency below is for the test suite only and does not ship.
[dev-dependencies]
motionvm-testutil.workspace = true

[lints]
workspace = true
